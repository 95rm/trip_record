<div class="container">

  <% if @trip_plan.errors.any? %>
    <%= @trip_plan.errors.count %>件のエラーが発生しました
    <ul>
      <% @trip_plan.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  <% end %>

  <%= form_with model: @trip_plan, url: trip_plans_path, method: :post do |f| %> <%#=form_withのURLとメソッドはmodelに情報が入っているか入っていないかでrailsが勝手に判断してcreateかupdateの判断をしてくれるから記述時なくてもよい%>

   <div class="row justify-content-center mb-3">
     <p>旅行のタイトル</p>
     <div class="col-8"><%= f.text_field :title_name %></div>
   </div>

   <div class="row justify-content-center mb-3">
     <p>日程</p>
     <div class="col-7"><%= f.date_field :first_day %> ～ <%= f.date_field :second_day %></div>
   </div>

   <div class="row justify-content-center mb-3">
     <p>期間</p>
     <div class="col-7"><%= f.number_field :number_day, class: "col-2 hoge", min: 0 %>日間</div>
   </div>

   <div class="row justify-content-center mb-3">
     <p>予算</p>
     <div class="col-7"><%= f.text_field :budget %>円</div>
   </div>

   <div class="row justify-content-center mb-3">
     <p>タグ</p>
     <div class="col-7"><%= f.text_field :name_tag, class: "col-5" %></div>
   </div>

   <div class="row justify-content-center">
     <%= f.submit '次へ', class: "rainbow-btn text-dark" %>
   </div>

  <% end %>

</div>
